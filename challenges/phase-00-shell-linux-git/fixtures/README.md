# Fixtures

`sample.log` has eight well-formed records and one malformed line, which is
line six. Counts by level: INFO 4, ERROR 2, WARN 1, DEBUG 1. Sources: api 4, web 2,
worker 2. Do not edit it — the tests assert on these numbers.
